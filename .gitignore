# Anarlog fork (added as submodule in Phase 2, not vendored)
/anarlog/

# Transcripts / meeting data — never commit (local-first, zero-telemetry)
*.transcript
*.wav
*.m4a
# ...but bundled app sounds ship with the repo
!MeetingCoach/MeetingCoach/Resources/*.wav
data/
transcripts/
**/private/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Node
node_modules/
dist/
build/

# Rust
target/

# Ollama binaries (too large for GitHub)
MeetingCoach/MeetingCoach/Resources/ollama/

# Xcode user state
*.xcuserstate
xcuserdata/

# macOS / editors
.DS_Store
.idea/
.vscode/
*.swp
tests/asr/audio/
tests/asr/.build/
tests/rubric/.build/
tests/rubric/yamlcheck/.build/
tests/detector/.build/
tests/ai/.build/
tests/demo/.build/
tests/asr/.out/
tests/nudges/.build/
tests/echo/.build/
tests/hygiene/.build/
tests/language/.build/
.wrangler/
tests/session/.build
tests/granola/.build/

# Encrypted-sharing regression binaries
tests/sharing/.build/
